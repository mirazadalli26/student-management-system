/**
 * Application configuration and startup components, including the
 * {@code DataInitializer} that provisions the default administrator account
 * and static-resource / web configuration.
 */
package com.sms.config;
